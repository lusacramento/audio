{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxi dev --https --ssl-key server.key --ssl-cert server.crt --port 3010",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.18.1",
    "tone": "^15.1.22",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  }
}
